syntax = "proto3";

package chat;
option go_package = "github.com/KonnorFrik/ChatServer;ChatServer";

// Create a chat with at least 2 participants
message CreateGroupChatRequest {
    int64 creator_id = 1;
    repeated int64 participant_id = 2;
}
